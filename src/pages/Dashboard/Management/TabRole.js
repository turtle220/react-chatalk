import React from "react";
import './index.css';

export default function TabRole(props) {
    const { children, value, index, ...other } = props;

    return (
        <div className='tab-content'>
            Role
        </div>
    );
}
